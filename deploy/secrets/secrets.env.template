# Secrets for the harness stack. Copy to secrets.env and fill values.
# This file is gitignored. Never commit secrets.env.
POSTGRES_USER=harness
POSTGRES_PASSWORD=changeme
POSTGRES_DB=harness

# Provider API keys — set the key for your chosen provider.
# For Anthropic/Claude (default):
ANTHROPIC_API_KEY=your-key-here

# To add more providers, add their key env vars here and
# set INFER_ALLOWED_PREFIXES=anthropic/,openai/,groq/ in the worker environment
